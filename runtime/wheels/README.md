# Backup Demo Wheels

Place pre-staged release wheels here before a live demo if you want an offline or low-risk install fallback.

Expected example:

```text
runtime/wheels/data_product_forge-<version>-py3-none-any.whl
```

That path is used by [fluid/demo/backup-demo.md](../../fluid/demo/backup-demo.md).
