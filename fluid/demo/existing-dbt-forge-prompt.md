# Existing dbt Forge Prompt

Use this during the optional AI-assisted enrichment step after `fluid import`.

> Refine this imported Snowflake telco data product so it represents a silver aggregated layer, keeps the existing dbt transformation intent, references the current dbt assets instead of replacing them, and stays ready for Airflow, Jenkins, standards export, and local Entropy Data publication.
