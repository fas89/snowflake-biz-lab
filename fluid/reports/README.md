# FLUID Reports

Store presenter notes, manual run summaries, or future FLUID execution reports here.

The folder is tracked so new users can see where report-like outputs belong, but the actual generated contents are intentionally ignored in normal work.
